# Bug 记录

> 记录已知问题，后续统一修复。

## 公告未在规定界面展示

- **发现时间**：2026-08-21
- **描述**：部分设置了 `show_on_publish=1` 或 `scroll=1` 的公告，在对应的发布页右侧/首页横幅位置没有正确展示。
- **影响范围**：公告展示功能（发布页公告面板、首页横幅滚动轮播）
- **状态**：⏳ 待定位修复
- **备注**：代码逻辑无报错，可能是前端渲染条件或接口返回数据过滤问题。后续排查时重点检查 `AnnouncementService.listPublish()` 和 `listLatest()` 的查询条件，以及前端 `ProductPublishView` 和 `HomeView` 的公告加载逻辑。

## 日志文件写入路径不正确

- **发现时间**：2026-08-21
- **描述**：部分环境的日志文件没有写入到预期的 `logs/app.log` 位置，可能写到了项目根目录或其他位置。
- **影响范围**：日志记录
- **状态**：⏳ 待定位修复
- **备注**：检查 `application.yml` 中的 `logging.file.name` 配置，确认 `./logs/app.log` 相对路径在启动时是否能正确解析到项目根目录下的 `logs/` 目录。重点关注 IDEA 启动时的工作目录（cwd）与命令行启动的差异。