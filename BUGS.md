# Bug 记录

> 记录已知问题，后续统一修复。

## 公告未在规定界面展示

- **发现时间**：2026-08-21
- **描述**：部分设置了 `show_on_publish=1` 或 `scroll=1` 的公告，在对应的发布页右侧/首页横幅位置没有正确展示。
- **影响范围**：公告展示功能（发布页公告面板、首页横幅滚动轮播）
- **状态**：⏳ 待定位修复
- **备注**：代码逻辑无报错，可能是前端渲染条件或接口返回数据过滤问题。后续排查时重点检查 `AnnouncementService.listPublish()` 和 `listLatest()` 的查询条件，以及前端 `ProductPublishView` 和 `HomeView` 的公告加载逻辑。

## 日志文件写入路径不正确

- **发现时间**：2026-08-21
- **描述**：服务器上日志未按预期写入 `/softservice/c2c/logs/`。
- **状态**：✅ 已修复（2026-08-21）——生产配置改为相对路径 `./logs/app.log`（与本地一致），由启动时工作目录决定实际位置
- **备注**：部署时注意宝塔启动目录；如希望日志固定到 `/softservice/c2c/logs/`，可在宝塔环境变量设 `APP_LOG_FILE=/softservice/c2c/logs/app.log`